message-delivery-client
--------------

This module holds a Spine 1.x-based Delivery client which is backed by the
[`message-delivery-server`](../server).

The project is developed as a stand-alone Gradle project while we'd like to depend on the servers'
Protobuf files but keep dependencies on Spine 1.x.
